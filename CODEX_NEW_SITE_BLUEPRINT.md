# Codex 新网站迁移蓝图

> 用途：把本文件复制到一个全新仓库，填写“项目变量”，再把“主 Prompt”交给 Codex。它迁移的是成熟静态网站的架构、内容模型、工作流和验收规则，不迁移当前网站的品牌、酒吧主题、人物信息、文章、图片、文案或配色。

## 1. 使用方法

1. 新建一个空 Git 仓库，把本文件放到仓库根目录。
2. 填写下一节的项目变量；不知道的项目可以先写“由 Codex 提案，等我确认”。
3. 在新项目的 Codex 任务中粘贴“第 3 节：可直接复制的主 Prompt”。
4. 让 Codex 先完成需求确认、信息架构和设计方向，再开始写代码。
5. 第一版验收后，再让 Codex 依据“第 15 节”创建项目自己的持久状态文件。

不要把当前项目直接复制后全局替换文字。原项目经过多轮追加，适合提炼契约，不适合作为新项目的干净代码底座。新项目应重新建立清晰的 HTML、CSS 和 JavaScript 文件，同时保留下面定义的数据边界、页面契约和验证流程。

## 2. 项目变量（先填写）

```text
站点名称：{{SITE_NAME}}
一句话定位：{{SITE_TAGLINE}}
站点所有者/署名：{{AUTHOR_NAME}}
目标用户：{{AUDIENCE}}
内容语言：{{LANGUAGE，例如 zh-CN}}
核心主题/隐喻：{{THEME_METAPHOR，例如天文台、温室、唱片店、航海日志}}
情绪关键词（3–6 个）：{{MOOD_WORDS}}
明确不要的视觉方向：{{VISUAL_AVOID}}
主栏目：{{PRIMARY_SECTIONS}}
内容分类：{{CONTENT_CATEGORIES}}
是否需要短内容流：{{YES_OR_NO}}
是否需要地点/项目档案与多次记录：{{YES_OR_NO}}
是否需要作品画廊：{{YES_OR_NO}}
是否需要友链/合作伙伴：{{YES_OR_NO}}
联系信息：{{CONTACT}}
GitHub 用户名：{{GITHUB_USER}}
仓库名：{{REPOSITORY_NAME}}
计划域名：{{DOMAIN_OR_GITHUB_PAGES_URL}}
现有素材路径：{{ASSET_PATHS_OR_NONE}}
参考网站/图片：{{REFERENCES_OR_NONE}}
首批真实内容：{{INITIAL_CONTENT_OR_PLACEHOLDERS}}
```

## 3. 可直接复制的主 Prompt

```text
你正在一个全新的仓库中构建成熟的个人内容网站。请把当前仓库中的 CODEX_NEW_SITE_BLUEPRINT.md 作为项目蓝图，并严格遵守以下要求。

一、任务目标

根据我填写的项目变量，从零构建一个可长期维护、可直接部署到 GitHub Pages 的网站。它可以继承蓝图里的架构思想，但所有品牌、主题元素、栏目命名、内容、图片、图标、配色、动效和文案都必须围绕新项目重新设计，不得复制蓝图来源网站的酒吧/鸡尾酒元素或任何专属内容。

二、开始编码前

1. 检查当前仓库、Git 状态、已有文件和可用素材。
2. 把已确认事实、缺失信息、合理假设、风险和下一步分开说明。
3. 只询问会显著改变信息架构、技术方案或视觉方向的问题；普通细节使用合理默认值并记录。
4. 先给出简洁的站点地图、页面职责、内容类型、视觉方向和分阶段实现计划。
5. 如果仓库已有 AGENTS.md 或其他项目指令，先读取并服从。
6. 未经我同意，不引入框架、包管理器、第三方运行时依赖、后端、数据库、CMS、登录系统或分析追踪。

三、默认技术方案

1. 默认使用语义化 HTML、共享 CSS 和原生 JavaScript，保证无需构建即可打开和部署。
2. 页面路径使用相对路径，兼容 GitHub Pages 的用户站点和项目站点。
3. 内容索引只保存元数据；长正文使用独立静态 HTML 文件。
4. 图片使用真实文件路径，不把大图或 base64 塞进 JavaScript 数据文件。
5. JavaScript 只负责渐进增强：数据列表、筛选、排序、轻量交互和可关闭动效。核心导航和主要内容不能依赖 JavaScript 才能访问。
6. 只有在内容规模、协作方式或功能需求确实超过静态方案时，才提出静态站点生成器或框架方案，并先说明迁移收益、成本和回滚方法。

四、交付标准

1. 先完成可导航的完整骨架，再完成视觉系统，再接入真实内容和数据渲染。
2. 桌面、平板、手机均需有明确布局策略，不能只靠整体缩小。
3. 支持 prefers-reduced-motion；装饰动效不得影响阅读、键盘操作和低性能设备。
4. 所有页面有独立 title；重要页面有 description；图片有合适 alt；外链使用安全 rel 属性。
5. 当前导航项使用 aria-current；交互控件有可理解的 aria-label、焦点态和禁用态。
6. 为空内容提供诚实的空状态，不伪造大量“真实”内容。
7. 提供 README：本地预览、目录说明、发布新内容、添加图片、修改主题、部署与回滚。
8. 提供 GitHub Pages 工作流和 .nojekyll；部署产物排除内部状态、草稿和仓库说明文件。
9. 完成后运行与改动相关的语法检查、链接/引用搜索、git diff --check、git diff 和 git status，并只报告实际执行过的验证。
10. 如果没有进行浏览器视觉检查，必须明确说明剩余视觉风险；如果我要求视觉验收，再检查关键页面和多个视口。

五、实现原则

1. 使用一套统一设计令牌，而不是在各页面散落颜色、间距和阴影常量。
2. 建立可复用的页头、导航、页脚、页面标题、卡片、按钮、标签、空状态、文章正文和档案记录样式契约。
3. 每个主栏目可以有独立视觉身份，但必须来自同一个母系统；不要为每页发明互不兼容的设计语言。
4. 动效是可选增强，不是识别度的唯一来源；关闭动效后网站仍需完整。
5. 数据进入 DOM 前进行转义；不要把不可信字段直接拼接成可执行 HTML。
6. 日期使用可排序格式 YYYY-MM-DD；需要精确时间时使用 YYYY-MM-DD HH:mm:ss。
7. 所有 id、slug、URL 在项目内唯一且稳定；发布后不随标题轻易变化。
8. 删除或替换旧文件前先搜索所有引用，并给出可恢复方法。

请先输出站点地图、内容模型、视觉方向、实施计划和需要我确认的关键选择；确认后再实施。实施过程中保持改动聚焦，完成一个阶段后验证并记录状态，不要在未解释的失败上继续叠加修改。
```

## 4. 推荐的干净目录结构

以下结构是默认方案。没有某种内容时应删除对应模块，不要为了“显得完整”保留无用途页面。

```text
/
├─ index.html                     # 首页
├─ archive.html                   # 综合内容归档
├─ about.html                     # 关于/联系
├─ gallery.html                   # 可选：作品或摄影
├─ friends.html                   # 可选：友链/合作伙伴
├─ status.html                    # 可选：当前状态
├─ collection.html                # 可选：地点、项目、书影音等档案列表
├─ article-<slug>.html            # 独立长文
├─ note-<slug>.html               # 可选：独立中短内容
├─ record-<slug>.html             # 可选：稳定档案页
├─ entry-<slug>-<date>.html       # 可选：某档案的一次记录
├─ assets/
│  ├─ styles.css                  # 令牌、组件、布局、响应式规则
│  ├─ site.js                     # 渲染与交互
│  ├─ content.js                  # 轻量内容元数据
│  ├─ feed.js                     # 可选：短内容流
│  ├─ brand/                      # Logo、徽章、图标
│  ├─ posts/<content-id>/         # 文章图片
│  ├─ gallery/                    # 作品图
│  └─ records/<record-id>/        # 档案相关图片
├─ _drafts/                       # 不发布的参考模板和草稿
├─ .github/workflows/pages.yml
├─ .nojekyll
├─ README.md
├─ AGENTS.md
└─ AGENT_*.md                     # 可选但推荐：长期 Codex 状态
```

如果页面越来越多，重复页头/页脚开始造成明显维护负担，可以在用户批准后增加一个无运行时依赖的生成脚本或静态站点生成器。不要在第一版为了消除少量重复就引入庞大工具链。

## 5. 信息架构原则

首页不是所有功能的堆叠，而是站点身份与主要入口的编辑结果。建议分为：

1. 品牌区：名称、定位、一个主要行动。
2. 主栏目入口：控制在 4–8 个，每个入口有清晰职责。
3. 当前/精选区：展示最有时效或最能代表站点的内容。
4. 最近内容：从元数据自动取最新 3–6 条。
5. 页脚：版权、联系、次级入口。

栏目设计规则：

- “长文”“札记”“短动态”按表达完整度区分，不仅按字数区分。
- 同一内容若属于综合归档，只保留一份数据源，在渲染层归一化，不要复制记录。
- 次级栏目不必全部进入顶部导航；移动端导航容量优先。
- 稳定实体和时间事件分开建模。例如“某家店/某个项目”是稳定档案，“某次到访/某次迭代”是独立事件。
- 空栏目可以显示简洁计划，但不要创建十几个无内容的装饰页面。

## 6. 通用内容模型

新项目可把下面对象改名为自己的领域名。字段不是越多越好；只保留页面真实使用的字段。

### 6.1 站点配置

```js
window.SITE_CONFIG = {
  name: "{{SITE_NAME}}",
  tagline: "{{SITE_TAGLINE}}",
  language: "zh-CN",
  author: "{{AUTHOR_NAME}}",
  recentLimit: 3,
};
```

### 6.2 长文

```js
window.SITE_POSTS = [
  {
    id: "stable-unique-id",
    title: "文章标题",
    category: "分类名",
    publishedAt: "2026-07-25",
    summary: "用于首页、归档和分享的简短摘要。",
    url: "article-stable-unique-id.html",
    cover: "assets/posts/stable-unique-id/cover.webp", // 可选
  },
];
```

### 6.3 中短内容

```js
window.SITE_NOTES = [
  {
    id: "note-id",
    title: "札记标题",
    category: "札记",
    publishedAt: "2026-07-25",
    mood: "可选状态",
    summary: "摘要",
    url: "note-note-id.html",
  },
];
```

归档需要同时展示长文和札记时，在 JavaScript 中合并并排序，不要把札记复制进长文数组。

### 6.4 稳定档案与多次事件

这个模式不仅适用于地点，也适用于项目、产品、书、专辑、课程或长期实验。

```js
window.SITE_RECORDS = [
  {
    id: "record-id",
    name: "档案名称",
    type: "类型",
    area: "地点或领域",
    url: "record-record-id.html",
    tags: ["标签一", "标签二"],
    oneLine: "长期判断",
    notes: "不会被单次事件覆盖的档案说明。",
    ratings: { quality: 8.5, value: 8, revisit: 9 }, // 可选
    entries: [
      {
        id: "record-id-20260725",
        no: 1,
        title: "第一次记录",
        label: "事件标签",
        date: "2026-07-25",
        score: 8.7,
        url: "entry-record-id-20260725.html",
        items: ["本次涉及的项目"],
        summary: "只描述这一次事件。",
      },
    ],
  },
];
```

长期总分应明确规则，例如“有效事件分数的平均值”；没有分数时显示“待评分”，不要把缺失值当成 0。

### 6.5 短内容流

```js
window.SITE_FEED = [
  {
    text: "简短内容。",
    label: "状态",
    emoji: "✦",
    publishedAt: "2026-07-25 20:30:00",
    detailUrl: "feed-detail-id.html", // 可选
  },
];
```

短内容正文应有明确上限。需要大图或长上下文时，列表只保留摘要，通过 `detailUrl` 进入独立详情页。数据量明显增长后，再升级为分页静态 JSON 或生成分页，不要一开始就引入数据库。

## 7. 页面契约

### 7.1 所有公共页面

- `<!doctype html>`、正确 `lang`、UTF-8、viewport。
- 独立 `title`；重要页面有 `meta description`。
- 统一页头、品牌链接、主导航和动效开关。
- 当前页面导航带 `aria-current="page"`。
- 一个明确的 `<main>`；标题层级不跳跃。
- 统一页脚；外链使用 `target="_blank" rel="noopener noreferrer"`。
- 共享 CSS 在 head；数据脚本先于主逻辑脚本加载。
- 资源版本号保持一致，或采用可维护的统一版本策略。

### 7.2 列表/归档页

- HTML 保留可识别的渲染容器，如 `data-post-list`。
- 筛选按钮使用真实 `button`，有活动态、键盘焦点和空结果。
- 排序逻辑集中在 JavaScript，不在多个页面各写一份。
- 卡片完整可点击时仍应保持可理解的链接文本。

### 7.3 独立文章页

- 正文始终存在独立 HTML 文件中。
- 标题区包含分类、标题、日期、摘要。
- 正文区支持段落、二级标题、引用、列表、代码和图片。
- 侧栏或文章尾部提供分类、文章信息与返回归档的单一主入口。
- 可以保留“余注/后记”，但不要重复摘要，也不要堆叠多个返回按钮。
- 图片放在 `assets/posts/<id>/`，包含尺寸合适的格式和 alt。

### 7.4 档案页

- 顶部展示长期信息。
- 单次事件按日期倒序排列，各自链接到独立详情。
- 新事件只追加，不覆盖旧事件。
- 总览卡与详情页使用同一 id、URL 和分数来源。

### 7.5 画廊页

- 使用真实 `<img>`、尺寸属性、alt 与说明文字。
- 首屏以下图片优先 `loading="lazy"`。
- 图片目录、命名、压缩和版权来源在 README 中说明。
- 没有作品时显示少量真实空状态，不伪造作品集。

## 8. JavaScript 职责与安全边界

`assets/site.js` 建议按职责分段：

1. 用户偏好：动效开关、`localStorage`、系统减少动态效果。
2. 数据工具：日期排序、数组归一化、URL 回退。
3. 安全输出：统一 `escapeHtml()`。
4. 内容渲染：最近内容、归档、分类页、中短内容。
5. 可选档案渲染：筛选、评分、事件时间线。
6. 可选短内容流：分页、详情链接。
7. 可选环境动效：只在允许动效且屏幕足够大时初始化。
8. 页面初始化：每个函数先检查目标容器是否存在，因此同一个脚本可安全加载到所有页面。

必须避免：

- 把未转义文本直接插入模板字符串。
- 在每个页面复制一套排序/筛选逻辑。
- 用 JavaScript 生成唯一导航路径。
- 在 `DOMContentLoaded` 之前假设页面节点必然存在。
- 动效循环在关闭后仍持续消耗资源。
- 把“主题切换”按钮用于与名称不符的功能；按钮名称应准确描述实际行为。

## 9. 视觉系统迁移规则

迁移的是系统，不是旧主题。

先确定：

- 一个核心隐喻。
- 3–6 个情绪词。
- 1 个主背景、1 个表面色、1 个正文色、1 个弱化色、1 个主强调色、最多 2 个辅助强调色。
- 标题、正文、数字/标签的字型角色。
- 圆角、边框、阴影、间距、内容宽度和动效时长令牌。

推荐令牌骨架：

```css
:root {
  color-scheme: dark; /* 或 light；按新主题决定 */
  --color-bg: #000;
  --color-surface: #111;
  --color-text: #f5f5f5;
  --color-muted: #aaa;
  --color-line: rgb(255 255 255 / 14%);
  --color-accent: #7dd3fc;
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --radius-sm: 0.5rem;
  --radius-md: 0.9rem;
  --shadow-card: 0 1rem 3rem rgb(0 0 0 / 24%);
  --content-wide: 72rem;
  --content-reading: 46rem;
  --motion-fast: 160ms;
  --motion-normal: 260ms;
}
```

每个栏目可以映射一个“身份元素”（颜色、符号、材质、编号或图形），但必须满足：

- 语义明确，不能只靠颜色区分。
- 图形使用统一画布和比例系统。
- 响应式时缩放整体，不随意改内部几何导致漂移。
- 页面身份不会压过页面标题和实际内容。
- 装饰层使用 `pointer-events: none`，不遮挡内容和焦点。

新项目应从干净 CSS 开始，按“令牌 → 基础元素 → 布局 → 组件 → 页面 → 响应式 → 减少动效”排序。不要把历史实验样式不断追加到文件末尾；被替代的规则应在确认无引用后删除。

## 10. 响应式与动效要求

至少设计并检查以下区间：

- 宽桌面：约 1180px 以上。
- 平板/小桌面：约 721–1179px。
- 手机：720px 以下。
- 小手机：480px 以下。

具体断点应由内容决定，而不是机械照抄数字。检查项目包括：

- 导航是否横向溢出或需要网格化。
- 首页栏目卡是否从多列转换成一列，而不是缩成无法阅读的小卡。
- 文章正文是否保持舒适行宽。
- 侧栏是否在平板/手机移到正文下方。
- 长英文、URL、标签和评分芯片是否自然换行。
- 固定侧栏是否在窄屏变成普通文档流。
- 图片是否溢出。

动效规则：

- 尊重 `prefers-reduced-motion: reduce`。
- 小屏默认可关闭复杂画布或指针效果。
- 开关状态可用 `localStorage` 持久化。
- 关闭后停止 `requestAnimationFrame`。
- 自定义指针只能作为装饰，不能替代系统指针和焦点反馈。

## 11. 内容发布工作流

### 新长文

1. 确定稳定 id/slug、标题、分类、日期、摘要。
2. 从干净文章模板创建 `article-<slug>.html`。
3. 图片放入 `assets/posts/<id>/`，检查引用和 alt。
4. 在内容索引新增一条元数据。
5. 检查首页最近内容、综合归档和分类页。
6. 更新统一资源版本（如果采用查询参数缓存破坏策略）。
7. 运行 HTML 引用检查与相关 JavaScript 语法检查。

### 新中短内容

流程与长文相同，但写入独立数组并使用独立模板；综合归档由渲染层合并数据。

### 新档案事件

1. 已有稳定档案时，只追加事件并创建独立事件页。
2. 新档案时，同时创建稳定档案页、档案数据和第一次事件。
3. 同步检查 record id、entry id、编号、日期、分数、URL 和关联内容元数据。
4. 特定类型需要列出本次项目时，使用明确字段，如 `drinks`、`dishes`、`tracks`；渲染层可归一为通用 `items`。

### 新短动态

保持简短；自动补充标签/emoji 前应符合站点语气。带大图时使用独立详情页，列表页不内嵌大图。

## 12. 缓存、链接与资源规则

- 无构建系统时，可给共享 CSS/JS 使用统一查询参数，例如 `?v=site-20260725-a`。
- 一次内容或逻辑发布只使用一个新版本值，并全站同步；不要让不同页面长期混用多个旧版本。
- 修改数据文件后也要刷新版本，否则首页/归档可能继续显示浏览器缓存。
- 发布前搜索所有旧版本值和断开的相对路径。
- SVG、PNG、JPG、WebP 等二进制资源必须使用二进制安全方式复制，不能通过文本重定向重建。
- 文件名使用稳定小写英文 slug；避免空格、临时名和随标题变化的路径。

## 13. GitHub Pages 部署基线

建议使用显式工作流，而不是只依赖隐式 Pages 构建。工作流应：

1. 在 `main` push 和手动触发时运行。
2. 给予 `contents: read`、`pages: write`、`id-token: write`。
3. 使用并发组，取消过时部署。
4. 生成干净 `_site` 目录。
5. 排除 `.git`、`.github`、`_site`、`_drafts`、README、AGENTS 和 AGENT 状态文件。
6. 在产物中创建 `.nojekyll`。
7. 上传 Pages artifact 并部署。

仓库如果是项目站点而非 `username.github.io` 用户站点，必须实际验证相对路径和站点 base path。不要宣称远程部署成功，除非确实检查过对应 Actions 运行和线上页面。

## 14. 验证清单

每次任务按风险选择检查，不要只跑命令不看输出。

```text
[ ] git status --short
[ ] git diff --stat
[ ] git diff -- .
[ ] git diff --check
[ ] node --check assets/site.js（相关时）
[ ] node --check assets/content.js（相关时）
[ ] node --check assets/feed.js（相关时）
[ ] 搜索新增 id、slug、URL、data-* 容器和字段名的所有引用
[ ] 搜索旧缓存版本
[ ] 检查所有本地 href/src 目标存在
[ ] 检查首页、归档、一个分类页、一个文章页、一个可选模块页
[ ] 检查键盘焦点、减少动效、空状态和无 JavaScript 退化
[ ] 检查桌面、平板、手机和小手机布局（仅在实际做过时声称通过）
[ ] 检查 GitHub Actions 与线上 URL（仅在实际部署后）
```

如果 Node 或 Git 不可用，记录准确限制，继续做文件级搜索和静态推理；不要用不等价的临时方法冒充验证。

## 15. 推荐的 Codex 持久状态系统

长项目不要只依赖聊天记录。建议在新项目建立以下文件，但内容必须描述新项目，不能复制当前项目的业务状态。

### `AGENTS.md`

只放强制工作规则：

- 项目技术边界和主入口。
- 启动恢复流程。
- 深层状态读取权限。
- 文件修改与安全限制。
- 验证偏好。
- 完成报告要求。

### 低令牌启动文件

- `AGENT_BRIEF.md`：当前架构、核心文件、最重要规则、已知风险，保持短小。
- `AGENT_HANDOFF.md`：最近完成、当前阶段、下一安全步骤。
- `AGENT_TODO.md`：未完成事项、阻塞、需用户确认的选择、固定验收清单。

### 按任务路由的操作文件

可按新项目实际工作类型建立：

- `AGENT_OP_CONTENT.md`
- `AGENT_OP_VISUAL.md`
- `AGENT_OP_FRONTEND.md`
- `AGENT_OP_RECORDS.md`
- `AGENT_OP_DEPLOY.md`
- `AGENT_OP_PROJECT_STATE.md`
- `AGENT_OP_RECOVERY.md`

每个操作文件只包含：适用范围、默认读取集合、何时允许深读、常见目标文件、验证命令、状态记录规则。

### 深层状态

- `AGENT_CONTEXT.md`：稳定架构、长期决策及原因。
- `AGENT_CHANGELOG.md`：近期重要改动与回滚提示。
- `AGENT_CHANGELOG_ARCHIVE.md`：旧历史，仅在审计/回滚时读取。

推荐启动流程：

1. 读 `AGENTS.md`。
2. 读 brief、handoff、todo。
3. 检查 Git 状态与当前 diff。
4. 选择一个或少数匹配的 operation 文件。
5. 只读任务相关源码。
6. 深层状态仅在架构不明、回滚、冲突或多文件结构变更时读取。
7. 修改后执行相关验证。
8. 整个操作只写一条简洁 changelog，并在工作流发生变化时更新 brief/handoff/todo。

状态文件不应变成业务内容副本。`BRIEF` 不能演变为完整历史，`HANDOFF` 不能无限追加所有文章摘要，`TODO` 要定期移除已经没有意义的旧任务。

## 16. 分阶段实施方案

### 阶段 0：确认

- 填完项目变量。
- 确认技术边界、目标用户、首批真实内容、部署目标。
- 确认哪些模块明确不做。

### 阶段 1：信息架构

- 站点地图。
- 页面职责。
- 内容类型及字段。
- URL 与文件命名策略。
- 导航优先级和移动端策略。

验收：不写复杂视觉前，所有主要页面能互相导航。

### 阶段 2：设计系统

- 视觉方向板、令牌、排版、卡片和页面身份。
- 首页与一个代表性详情页。
- 桌面/平板/手机布局。

验收：先确认母系统，再扩展全部页面。

### 阶段 3：内容系统

- 数据索引、最近内容、归档、分类筛选、空状态。
- 独立正文模板。
- 可选札记、档案、短内容流。

验收：新增一条内容只需新增页面、元数据和资源，不需要修改多份渲染逻辑。

### 阶段 4：成熟度

- 可访问性、SEO、性能、减少动效。
- 真实图片及压缩策略。
- 404、favicon、Open Graph 等可选生产细节。
- README 与状态文件。

### 阶段 5：部署

- Pages 工作流。
- Actions 检查。
- 线上链接、资源、移动端复核。

## 17. 完成定义

只有同时满足下列条件，第一版才算完成：

- 所有已确认栏目都有明确入口或明确不进入主导航的理由。
- 没有复制来源网站的专属品牌、文案、素材或主题符号。
- 数据源、正文页面和渲染逻辑职责分离。
- 所有主要页面均有内容或诚实空状态。
- 移动端不是桌面版的机械缩小。
- 关闭 JavaScript 或动效后，核心阅读和导航仍可用。
- 资源路径、链接、id 和数据字段经过搜索核对。
- 相关语法检查与 Git diff 检查通过，或准确记录限制。
- README 足以让未来维护者新增内容和部署。
- Codex 状态文件反映当前阶段、风险和下一安全步骤。
- 实际检查部署后才声称网站已上线。

## 18. 常见失败模式

- 直接复制旧站后只换文字，导致主题、类名和内容模型仍被旧隐喻绑架。
- 一开始引入框架，却没有内容规模或交互需求支撑。
- 把全文、图片或 base64 塞入一个巨大 JavaScript 文件。
- 稳定实体和单次事件混在同一条记录，下一次更新覆盖旧历史。
- 多个页面手写不同版本的导航、筛选和页脚却没有一致性检查。
- CSS 只在文件尾持续追加覆盖层，旧规则从不清理。
- 动效在手机、减少动态设置或后台标签页中仍持续运行。
- 用“占位作品”伪装成真实作品集。
- 修改数据却忘记缓存版本，误以为渲染失败。
- 只说“测试通过”，但没有给出实际命令和检查范围。
- 在未检查引用时删除兼容页、素材或部署文件。

## 19. 新项目首次交付时要求 Codex 报告

最终报告必须包含：

1. 使用了哪些项目指令和状态文件。
2. 创建/修改了哪些页面、数据、样式、逻辑和部署文件。
3. 哪些内容是用户确认，哪些是合理假设。
4. 实际执行了哪些验证及结果。
5. 是否进行了视觉检查；若没有，剩余视觉风险是什么。
6. 尚未完成或需要真实素材的部分。
7. 如何回滚本次改动。
8. 下一步最值得做的一件事。

---

这份蓝图提炼自一个长期维护的静态个人网站。最值得迁移的不是某种配色或主题装饰，而是四个边界：内容元数据与正文分离、稳定档案与时间事件分离、基础内容与渐进增强分离、聊天上下文与仓库持久状态分离。
